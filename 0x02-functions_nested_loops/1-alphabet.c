#include "main.h"
#include <stdio.h>

/**
 * main - function that prints lowercase
 *
 * Return: 0 (Success)
 */

void print_alphabet(void);
{
	char d;

	for (d = 'a'; d <= 'z'; d++)

	{
		_putchar(b);
	}
	_putchar('\n');
}
